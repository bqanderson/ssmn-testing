function(initPhases){
  // <turn initPhases to array of milestone values>
}

initPhases.reduce(function(array, phase){
  var values = phase.map(function(milestone){

  })
})


// OR
$scope.$watchCollection(function(){
  for (var i = 0; i < array.length; i++) {
    for (var j = 0; j < array.length; j++) {
      array[i]
    }
  }
})
